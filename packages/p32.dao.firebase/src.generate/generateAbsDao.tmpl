import { {{modelName}}Model } from "@sejong/model";
import { AbsDao } from "./AbsDao";

// 자동생성된 코드
export abstract class Abs{{modelName}}Dao extends AbsDao<{{modelName}}Model> {

    protected getTableName(): string {
        return "{{modelName}}";
    }
}